/**
 * A method who verify if the value is odd
 * @param {number} value - The value to verify
 * @returns {boolean} **true** if the *value* is odd; **false** if the *value* is even;
 **/
export function isOdd(value: number): boolean {
  return value % 2 === 1;
}
